#!/usr/bin/env bash
echo "auth_inject(shim): script yok. Cypture ile login ol → cyp_login(login_url,username,password) VEYA cyp_create_replay_session; dönen SESSIONID'yi cyp_send_request'e kat. İki-rol A/B için AYRI session (user vs admin/user2)."
exit 0
